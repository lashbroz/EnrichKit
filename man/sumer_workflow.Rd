% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sumer.R
\name{sumer_workflow}
\alias{sumer_workflow}
\title{Prepare SUMER Inputs, Write Config Template, Run, and Read Modules}
\usage{
sumer_workflow(
  enrichment,
  gene_sets,
  out_prefix,
  config_file = paste0(out_prefix, "_sumer_config.json"),
  output_name = paste0(out_prefix, "_sumer_output"),
  pathway_col = "pathway",
  weight_col = "weights",
  fdr_col = "fdr",
  direction_col = "dir",
  min_abs_weight = NULL,
  fdr_threshold = NULL,
  top_n = NULL,
  selection_summary_file = paste0(out_prefix, "_selection_summary.tsv"),
  deduplicate = TRUE,
  write_config = TRUE,
  config_extra = list(),
  top_num = 100,
  platform_name = output_name,
  platform_abbr = output_name,
  run = FALSE,
  sumer_fun = NULL,
  expected_output_dir = output_name,
  overwrite = FALSE,
  read_modules = TRUE
)
}
\arguments{
\item{enrichment}{A data frame with pathway-level enrichment results.}

\item{gene_sets}{Named list of pathway gene sets.}

\item{out_prefix}{Output prefix, without extension.}

\item{config_file}{Output or existing SUMER config file.}

\item{output_name}{Output name/directory passed to SUMER.}

\item{pathway_col}{Column containing pathway names.}

\item{weight_col}{Optional column containing SUMER weights.}

\item{fdr_col}{FDR column used to derive weights when \code{weight_col} is absent.}

\item{direction_col}{Direction column used to derive weights when \code{weight_col}
is absent. Values should be signed numeric direction/effect indicators.}

\item{min_abs_weight}{Optional minimum absolute weight to retain.}

\item{fdr_threshold}{Optional FDR threshold to retain pathways before SUMER
input writing. Requires \code{fdr_col}.}

\item{top_n}{Optional number of strongest pathways to retain after other
filters. Pathways are ranked by absolute SUMER weight.}

\item{selection_summary_file}{Optional TSV file documenting the number of
pathways retained at each SUMER-selection step. If \code{NULL}, no summary file
is written.}

\item{deduplicate}{Keep only the strongest row per pathway.}

\item{write_config}{If \code{TRUE}, write a config template before running.}

\item{config_extra}{Named list of additional config fields.}

\item{top_num}{Number of top pathways in the SUMER config template.}

\item{platform_name}{Human-readable platform name in the SUMER config.}

\item{platform_abbr}{Short platform abbreviation in the SUMER config.}

\item{run}{If \code{TRUE}, call \code{\link[=run_sumer]{run_sumer()}}. If \code{FALSE}, only prepare files.}

\item{sumer_fun}{Optional function to call. If omitted, EnrichKit searches
for a loaded \code{sumer()} function.}

\item{expected_output_dir}{Directory expected to contain SUMER output files.}

\item{overwrite}{If \code{FALSE}, error when \code{expected_output_dir} already exists.}

\item{read_modules}{If \code{TRUE}, read SUMER modules after running.}
}
\value{
A list with \code{prep}, \code{config_file}, \code{run}, and \code{modules}.
}
\description{
High-level convenience wrapper for the full SUMER handoff. It writes SUMER
input files, writes a config template if requested, calls external SUMER, and
reads module outputs. Use \code{run = FALSE} to stop after creating the files.
}
