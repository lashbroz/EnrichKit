% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sumer.R
\name{get_sumer.data}
\alias{get_sumer.data}
\alias{get_sumer_data}
\title{Compatibility Wrapper for get_sumer.data}
\usage{
get_sumer.data(
  enrichment,
  gene_sets,
  out_prefix,
  pathway_col = "pathway",
  weight_col = "weights",
  fdr_col = "fdr",
  direction_col = "dir",
  min_abs_weight = NULL,
  fdr_threshold = NULL,
  top_n = NULL,
  selection_summary_file = paste0(out_prefix, "_selection_summary.tsv"),
  deduplicate = TRUE
)

get_sumer_data(
  enrichment,
  gene_sets,
  out_prefix,
  pathway_col = "pathway",
  weight_col = "weights",
  fdr_col = "fdr",
  direction_col = "dir",
  min_abs_weight = NULL,
  fdr_threshold = NULL,
  top_n = NULL,
  selection_summary_file = paste0(out_prefix, "_selection_summary.tsv"),
  deduplicate = TRUE
)
}
\arguments{
\item{enrichment}{A data frame with pathway-level enrichment results.}

\item{gene_sets}{Named list of pathway gene sets.}

\item{out_prefix}{Output prefix, without extension.}

\item{pathway_col}{Column containing pathway names.}

\item{weight_col}{Optional column containing SUMER weights.}

\item{fdr_col}{FDR column used to derive weights when \code{weight_col} is absent.}

\item{direction_col}{Direction column used to derive weights when \code{weight_col}
is absent. Values should be signed numeric direction/effect indicators.}

\item{min_abs_weight}{Optional minimum absolute weight to retain.}

\item{fdr_threshold}{Optional FDR threshold to retain pathways before SUMER
input writing. Requires \code{fdr_col}.}

\item{top_n}{Optional number of strongest pathways to retain after other
filters. Pathways are ranked by absolute SUMER weight.}

\item{selection_summary_file}{Optional TSV file documenting the number of
pathways retained at each SUMER-selection step. If \code{NULL}, no summary file
is written.}

\item{deduplicate}{Keep only the strongest row per pathway.}
}
\value{
A list containing written file paths and the filtered SUMER input.
}
\description{
Compatibility wrapper around \code{\link[=prepare_sumer_input]{prepare_sumer_input()}}. This preserves the
older HOPE/KidsFirst-style function name \code{get_sumer.data()} while routing the
implementation through the explicit EnrichKit SUMER input writer.
}
\details{
Use this when migrating older analysis scripts that called \code{get_sumer.data}.
New code can call either \code{prepare_sumer_input()} or the snake-case alias
\code{get_sumer_data()}.
}
